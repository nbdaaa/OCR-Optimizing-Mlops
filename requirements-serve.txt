# ── Serving deps — installed on the vLLM GPU instance only ───────────────────
# The instance uses the vllm/vllm-openai image, so vLLM (+ torch/transformers)
# is ALREADY present — do NOT reinstall it here (slow + risks version conflict).
# We only add the lightweight bits vllm_server.py needs to fetch the adapter:
#
# mlflow-skinny (not full mlflow): the full package drags in matplotlib/flask/
# scikit-learn which backtrack-conflict with the image's pins. The skinny client
# still has MlflowClient + download_artifacts — all we need here.
mlflow-skinny==2.13.0
boto3>=1.34.0
python-dotenv>=1.0.0
huggingface-hub>=0.23.0
