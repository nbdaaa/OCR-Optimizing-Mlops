# ── API & Infrastructure ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0
mlflow==2.13.0
boto3>=1.34.0
# datasets <3 and pyarrow <16: mlflow 2.13 pins pyarrow<16; datasets 3.x/5.x
# require pyarrow>=21 → hard conflict. numpy<2 also required by mlflow 2.13.
datasets>=2.19.0,<3
pyarrow>=14,<16
huggingface-hub>=0.23.0
wandb>=0.17.0
prometheus-client>=0.20.0
python-multipart>=0.0.9
pydantic>=2.0.0
psycopg2-binary>=2.9.9
requests>=2.31.0
python-dotenv>=1.0.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
httpx>=0.27.0
Pillow>=10.0.0
imagehash>=4.3.1
numpy>=1.26.0,<2

# ── PDF ingest (self-labeling data versioning via the inference server) ───────
pymupdf>=1.24.0
