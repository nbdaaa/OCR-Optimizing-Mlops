# UI deps — run locally, talks to the VM API
streamlit>=1.36.0
requests>=2.31.0

# Extra deps for the "Tạo version từ PDF" mode (runs the client-side ingest
# in-process via src.data.pdf_versioning). Skip if you only use the HF-Hub mode.
pymupdf>=1.24.0
boto3>=1.34.0
pandas>=2.0.0
pyarrow>=14,<16
imagehash>=4.3.1
Pillow>=10.0.0
mlflow==2.13.0
datasets>=2.19.0,<3
python-dotenv>=1.0.0
