[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: requires MinIO + MLflow running (docker-compose up)
    e2e: requires full stack running

addopts = -v --tb=short
