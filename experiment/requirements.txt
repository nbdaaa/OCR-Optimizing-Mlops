# Experiment UI (Gradio) — run locally, talks to the VM serving LB + API.
gradio>=4.0.0
requests>=2.31.0
pandas>=2.0.0
pillow>=10.0.0
# DocTags rendered by a small custom converter (this model emits HTML tables,
# not OTSL) — no docling-core needed.
