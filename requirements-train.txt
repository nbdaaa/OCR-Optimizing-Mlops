# ── GPU / Training deps — install on Vast.ai instance only ───────────────────
# Versions pinned to the set verified working on RTX 3090 (24GB) with
# granite-docling-258M. Do NOT loosen without re-testing:
#   - transformers==4.53.0  : 5.x changed processor loading → can't load the
#                             granite-docling (Idefics3) image processor.
#   - mlflow / datasets / numpy / pyarrow constraints come from requirements.txt
#     (mlflow 2.13 pins pyarrow<16 + numpy<2; datasets must stay <3).
#   - vllm is NOT a training dep (serving only) — intentionally excluded.
-r requirements.txt

torch>=2.3.0
transformers==4.53.0
peft>=0.11.0
trl>=0.8.0
accelerate>=0.30.0
jiwer>=3.0.4
