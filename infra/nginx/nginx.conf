events {}

http {
    # Serving LB config is provided as conf.d/default.conf, generated by the host
    # scaler (colab_pool_scaler.py): split_clients → Cloudflare tunnels of the
    # active vLLM pool. No static upstream — the pool scales 0↔2 dynamically.
    include /etc/nginx/conf.d/*.conf;
}
