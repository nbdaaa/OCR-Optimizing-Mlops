global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: vllm
    file_sd_configs:
      - files:
          - /etc/prometheus/targets.json
        refresh_interval: 15s
    metrics_path: /metrics
